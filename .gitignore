# Hugo build output (regenerated by Cloudflare Pages on deploy)
public/

# Hugo lock + image processing cache
.hugo_build.lock
resources/_gen/

# Local dev tooling (Claude Code launch config)
.claude/

# OS / editor cruft
.DS_Store
Thumbs.db
